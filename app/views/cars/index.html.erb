<!-- <div class="d-banner">
 <div class="container">
  <div class="row">
    <div class="col-xs-12">
     <h3 class="text-center list-title">Results for your request</h3>

     <table class= "table">
           <% @cars.each do |car| %>
           <ul>
                <% if car.photo %>
                <%= cl_image_tag car.photo.path, height: 35, width: 35, crop: :fill %>
                <% end %>
 -->                <%= car.brand %> <%= car.model %> - <%= car.color %>
      <!--      <%= car.description %>      -->
                <%= link_to "Show car details ", car_path(car) %>
           </ul>
<!--            <% end %>
     </table>
    </div>
   </div>
  </div>
</div> -->

<div class="wrapper-bottom">
  <div class="row">
  <% @cars.each do |car| %>
    <div class="col-xs-12 col-md-6 col-lg-4 car-card">
      <% if car.photo %>>
      <%= cl_image_tag car.photo.path, height: 35, width: 35, crop: :fill, class: "card" %>
      <% end %>
      <div class="card-content">
        <%= car.model %>
      </div>
    </div>
  <% end %>
  </div>
</div>
